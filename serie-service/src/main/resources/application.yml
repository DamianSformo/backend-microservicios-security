spring:
  application:
    name: serie-service

  cloud:
    config:
      discovery:
        enable: true
        id-service: config-service

  rabbitmq:
    username: guest
    password: guest
    host: localhost
    port: 5672

queue:
  serie:
    name: SerieQueue
